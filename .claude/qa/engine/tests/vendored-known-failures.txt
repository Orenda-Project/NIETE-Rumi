# vendored-known-failures.txt — assertions in the VENDORED NIETE bash suites that already failed at the pinned
# source sha (Orenda-Project/NIETE-Rumi sandbox@de93aee5), measured 2026-09-17 in a detached worktree with the
# UNTOUCHED tests (bd-9157r). They are test-vs-spec drift owned by NIETE-Rumi, not engine regressions.
#
# tests/run-all.sh treats a FAIL in this list as KNOWN, a FAIL not in it as a REGRESSION (exit 1), and a listed
# assertion that now PASSES as FIXED (printed; remove the line). Format: <suite path><TAB><assertion text>.
hooks/e2e-autorun-gitmarker.test.sh	session marker is the one nudged
hooks/e2e-phase1-gate.test.sh	…saying it is not committed
hooks/e2e-phase1-gate.test.sh	…with the exact commit to make
hooks/e2e-phase1-gate.test.sh	stop after the spec is COMMITTED → silent
hooks/e2e-phase1-gate.test.sh	…and --clear now works
hooks/e2e-autorun.test.sh	--clear removes the marker
hooks/e2e-autorun.test.sh	--clear removes the marker
hooks/e2e-autorun.test.sh	--clear removes the sync brief
hooks/spec-sync-pipeline.test.sh	2. duplicate scenario name is caught
hooks/spec-sync-pipeline.test.sh	3. ...and named as E-DUPNAME
hooks/spec-sync-pipeline.test.sh	1. the spec now covers the new row
hooks/spec-sync-pipeline.test.sh	2. and the renamed opener
hooks/spec-sync-pipeline.test.sh	8. and it nudges only once
hooks/spec-sync-pipeline.test.sh	3. with a reason it passes
